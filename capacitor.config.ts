import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.example.tesladetailing',
  appName: 'Tesla Detailing',
  webDir: 'out',
  bundledWebRuntime: false,
};

export default config;
